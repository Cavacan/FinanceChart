<h1>Products</h1>

<%= line_chart @products.group(:price).sum(:amount) %>


<% @products.each do |product| %>
  <%= render product %>
<% end %>

<%= pie_chart @dd %>

<%= link_to "New Product", new_product_path %>
<br>
<%= link_to "return to TOP", root_path %>
